# Backend environment variables for production deployment
# Set MONGO_URI to your MongoDB Atlas or cloud MongoDB connection string.
# Example:
# MONGO_URI=mongodb+srv://<user>:<password>@cluster0.mongodb.net/clinical-doc-app?retryWrites=true&w=majority

MONGO_URI=
PORT=5000
